#include <stdio.h>

int main() {
    int num[5];
    
    printf("Enter your numbers");
    
    for(int i=0; i<5; i++)
    scanf("%d", &num[i]);
    
    for(int i=4; i>=0; i--)
    printf("\n %d", num[i]);
    return 0;
}
